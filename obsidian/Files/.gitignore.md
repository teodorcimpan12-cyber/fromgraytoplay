---
tags: [file, root, git]
file: .gitignore
---

# `.gitignore`

Excludes:
- `node_modules/` — only relevant for [[vite-next/README|vite-next/]]
- `.env`, `.env.local`, `.env.*.local` — Firebase keys for vite-next/
- `dist/`, `dist-ssr/`, `.vite/`, `coverage/`
- OS noise: `.DS_Store`, `*.log`
- Editor settings: `.idea/`, `.vscode/*` (with `extensions.json` allowed)

The shipped root has no build artifacts to ignore — everything lives in
[[Files/index.html|index.html]] and [[Files/manifest.json|manifest.json]].
